Stop the Simulator Inspector:

1. Call the `inspector_stop` MCP tool to close the overlay
2. Run `./scripts/simulator-inspector-off.sh` to kill any remaining overlay processes
3. Confirm the inspector has been stopped
